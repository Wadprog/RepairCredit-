import {} from "../consts";
import api from "../../utils/api";

export const getAccessDetails = () => {
  try {
  } catch (error) {}
};

export const setAccessDetails = clientId => {
  try {
  } catch (error) {}
};
